# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: specs/smoke.spec.ts >> Smoke Tests >> @smoke admin login page renders
- Location: tests/specs/smoke.spec.ts:18:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('input[type="email"]')
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('input[type="email"]')

```

```yaml
- heading "No encontrado" [level=1]
- paragraph: La página que buscas no existe o ya no está disponible.
- link "Volver al portal":
  - /url: /portal
- alert
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Smoke Tests', () => {
  4  |   test('@smoke portal loads without crash', async ({ page }) => {
  5  |     await page.goto('/portal');
  6  |     
  7  |     await expect(page).not.toHaveURL(/error|500|404/);
  8  |   });
  9  | 
  10 |   test('@smoke login page renders', async ({ page }) => {
  11 |     await page.goto('/portal/login');
  12 | 
  13 |     await expect(page.locator('input[type="email"]')).toBeVisible();
  14 |     await expect(page.locator('[data-testid="password-input"]')).toBeVisible();
  15 |     await expect(page.locator('button[type="submit"]')).toBeVisible();
  16 |   });
  17 | 
  18 |   test('@smoke admin login page renders', async ({ page }) => {
  19 |     await page.goto('/admin/login');
  20 | 
> 21 |     await expect(page.locator('input[type="email"]')).toBeVisible();
     |                                                       ^ Error: expect(locator).toBeVisible() failed
  22 |     await expect(page.locator('[data-testid="password-input"]')).toBeVisible();
  23 |     await expect(page.locator('button[type="submit"]')).toBeVisible();
  24 |   });
  25 | });
```