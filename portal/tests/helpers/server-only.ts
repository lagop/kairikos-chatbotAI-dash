// Stub for the `server-only` Next.js package. Unit tests run in node and
// don't need the production guard against accidental client-side imports,
// so we resolve the specifier to this empty module.
export {};
