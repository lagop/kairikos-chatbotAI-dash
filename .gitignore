node_modules/
dist/
build/

# Local env files — never commit
.env
.env.local
.env.*.local

# Local-only Supabase auth shim used in development. Do NOT apply to prod.
supabase/tests/_local_auth_shim.sql

# Staging-apply artifacts (real schema dumps + run logs from the staging
# Supabase project). Never committed — they reference the real project
# structure and may contain operator-handleable metadata.
supabase/tests/artifacts/

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Backup / scratch
*.bak
*.log
!supabase/tests/chatbot_clients_rls_smoke.run.log

# KAIA-719 rev 3 — local artifacts and run logs
.scratch/
portal/test-results/
portal/tsconfig.tsbuildinfo
portal/tsconfig.kiaia755.tsbuildinfo
supabase/tests/chatbot_clients_rls_smoke.run.log
.vercel
