# scripts/secrets-allowlist.txt
# One path per line. Each path is excluded from the secrets scan.
# Every entry must include a brief reason on the same line, after #.
# Adding an entry here is a CTO-level decision; the file is reviewed in PRs.
#
# See KAIA-1597 for the guardrail.
docs/runbook-KAIA-740.md            # uses <db-password> and <pw> placeholders in docs examples
docs/runbook-KAIA-1108.md           # references op:// URIs and example envs for the rotate-now worker
supabase/scripts/README.md          # uses <pw> and <STAGING-REF> placeholders in setup docs
portal/tests/unit/prisma-pg-flags.test.ts  # KAIA-2872 fixture URLs; passwords are literal "p"/"secret" tokens, not real credentials — the project ref/host are already public in STAGING.md and multiple runbooks
.github/workflows/ci.yml            # ephemeral CI Postgres service; user/password are both the literal word "placeholder"
scripts/verify-stripe-webhook.sh    # localhost-only dev DB default (matches .env.example); not a real credential
